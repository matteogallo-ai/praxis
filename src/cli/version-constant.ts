/**
 * Single source of truth for the CLI version string.
 *
 * Kept in a stand-alone module so tests can import it without triggering
 * the CLI dispatcher.
 */
export const PRAXIS_VERSION = "0.7.0";
